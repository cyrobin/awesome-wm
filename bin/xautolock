#!/bin/sh

icon="$1"
notify=$(dirname $(readlink -f "$0"))/notify-lock


exec xautolock \
    -time 10 \
    -locker "i3lock -n -i $HOME/.config/awesome/wallpaper/background.png" \
    -notify 10 \
    -notifier "$notify $icon"
